# Copy to .env.local (gitignored). Leave CUSTOM_API_KEY blank to run without LLM.
CUSTOM_API_KEY=
CUSTOM_API_BASE_URL=https://riyckji.abc-tunnel.us/v1
# Optional: override the model id (defaults to cx/gpt-5.5).
CUSTOM_API_MODEL=cx/gpt-5.5
VERIFIER_CONTACT_EMAIL=you@example.com
